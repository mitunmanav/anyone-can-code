---
satisfies: [R5, R11, R13, R22, R23]
---

## Description
Add durable persona and project-state foundation for ACC. Setup must create or repair builder/developer/mixed persona settings and workflow state before intake runs.

**Size:** M
**Files:**
- `plugins/anyone-can-code/scripts/setup.py`
- `plugins/anyone-can-code/scripts/doctor.py`
- `plugins/anyone-can-code/skills/setup/SKILL.md`
- `plugins/anyone-can-code/reference/project/AGENTS.md`
- `.codex/anyone-can-code/settings/preferences.json` (generated by setup)
- `.codex/anyone-can-code/state/workflow.json` (generated by setup)

## Approach
- Extend existing setup layout instead of adding a second state store.
- Keep defaults in `DEFAULT_PREFERENCES`; persona should be explicit, not inferred silently.
- Doctor should warn when persona/project state, settings defaults, or repo mode are missing and pass when setup repaired them.
- Preserve Windows-first Python execution.

## Investigation targets
**Required** (read before coding):
- `plugins/anyone-can-code/scripts/setup.py:34` - existing default preferences.
- `plugins/anyone-can-code/scripts/setup.py:102` - project bootstrap flow.
- `plugins/anyone-can-code/scripts/doctor.py:164` - project layout health check.
- `plugins/anyone-can-code/skills/setup/SKILL.md` - setup user-facing contract.

**Optional** (reference as needed):
- `plugins/anyone-can-code/README.md` - local data layout and setup model.
- `plugins/anyone-can-code/VALIDATION.md` - validation checklist.

## Key context
Doctor currently reports project bootstrap missing when `.codex/anyone-can-code/` has not been created. This task should make setup state explicit before intake tasks rely on it.

## Acceptance
- [ ] `setup.py` stores persona mode with allowed values `builder`, `developer`, and `mixed`.
- [ ] Default persona is safe for non-technical builders unless user configured otherwise, with caveman communication default.
- [ ] `doctor.py --json` reports persona/project-state readiness.
- [ ] Setup docs explain persona once, then auto-configuration after.
- [ ] Windows command `python plugins\anyone-can-code\scripts\doctor.py --json` still runs.

## Done summary
Added durable builder persona/project-state defaults, setup repair behavior, doctor readiness checks, setup docs, and focused unittest coverage.
## Evidence
- Commits:
- Tests: python -m unittest plugins\anyone-can-code\tests\test_project_state.py, python plugins\anyone-can-code\scripts\doctor.py --json, python -m py_compile plugins\anyone-can-code\mcp\server.py plugins\anyone-can-code\scripts\setup.py plugins\anyone-can-code\scripts\doctor.py, python .flow\bin\flowctl.py validate --spec fn-1-define-project-direction --json
- PRs: