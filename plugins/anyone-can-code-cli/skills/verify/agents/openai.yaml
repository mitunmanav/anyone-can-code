interface:
  display_name: "ACC check"
  short_description: "Verify work with real proof"
  default_prompt: "Use $verify to check the built code before marking a task done."

policy:
  allow_implicit_invocation: true
