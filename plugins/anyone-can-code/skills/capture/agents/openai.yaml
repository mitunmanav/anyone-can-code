interface:
  display_name: "Capture"
  short_description: "Explicit-only decisions and blockers recording — NOT a prompt monitor"
  default_prompt: "Use $capture to record an important decision or blocker."
