interface:
  display_name: "ACC wiki"
  short_description: "Look at or repair auto project memory"
  default_prompt: "Show what ACC remembers for this project, or run memory doctor if something looks broken."
