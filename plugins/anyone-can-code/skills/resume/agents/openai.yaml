interface:
  display_name: "Resume"
  short_description: "Resumes, reconstructs, repairs, or abandons stale workflow state using the local project artifacts."
  default_prompt: "Use $resume when the workflow needs to continue or recover."
