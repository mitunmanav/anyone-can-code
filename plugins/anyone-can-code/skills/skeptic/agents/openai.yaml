interface:
  display_name: "ACC skeptic"
  short_description: "Read-only adversarial review of a diff"
  default_prompt: "Use $skeptic for a read-only second pass on the git diff or named files. Does not replace $verify."

policy:
  allow_implicit_invocation: false
