interface:
  display_name: "Learn"
  short_description: "Captures reinforced learnings through portable Markdown memory, with tiny local fallback ledgers only."
  default_prompt: "Use $learn after verified work or explicit user feedback to record durable learnings."

policy:
  allow_implicit_invocation: true
