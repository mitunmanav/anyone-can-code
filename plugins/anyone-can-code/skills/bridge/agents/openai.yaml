interface:
  display_name: "Bridge"
  short_description: "Installed plugin detection — routes tasks, prevents duplication"
  default_prompt: "Use $bridge to check if another installed plugin can handle the current task."
